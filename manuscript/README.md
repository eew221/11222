# Manuscript materials

`main_v27.tex` is the current manuscript source. The compiled PDF is retained only in the authors' local submission workspace until manual qualitative-image redaction and organization authorization have been completed.

The source refers to a qualitative figure derived from a self-collected, restricted image collection. That source image and the compiled PDF are intentionally not included in this public repository until manual redaction and organization authorization have been completed. A fully clean-room rebuild of the qualitative figure requires authorized local data access.

The public `figures/v23/` directory contains only non-sensitive process, aggregate-risk, feasibility, and audit-summary plots. The non-image analysis figures can be regenerated with the analysis and plotting scripts after the authorized data, manifests, checkpoints, and prediction caches have been placed at the paths configured for the local run.
